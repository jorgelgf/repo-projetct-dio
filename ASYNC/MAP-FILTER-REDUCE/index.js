const maca = {
  value: 2,
};
const laranja = {
  value: 3,
};

function mapsemThis(arr) {
  return arr.map((item) => item * 2);
}

const num = [1, 2, 4, 6, 8];
console.log(mapsemThis(num));


function filtraPares{

  return arr.filte((callback))
}

function callback(item){
  return item%2==0
}

const meuArre